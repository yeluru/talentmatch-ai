# Copy this file to `.env` for production/cloud usage:
#   cp env.example .env
#
# Local dev note:
# - When running on localhost, the app forces local Supabase (see src/integrations/supabase/client.ts),
#   so you can usually leave `.env` unset for local development.
# - If Edge Functions return 503 (Kong → Docker edge-runtime), set the direct URL so the app
#   calls your host's `supabase functions serve` instead. Use the port shown when you run that command (often 8081):
#   VITE_SUPABASE_FUNCTIONS_DIRECT_URL=http://127.0.0.1:8081
#
# Production: set VITE_* in Render (or your host) env; set the rest in Supabase Edge Function secrets.
# See docs/PRODUCTION-DEPLOYMENT.md for full deployment steps.

# --- Frontend (Vite) — set in Render / host env ---
# Supabase (cloud / production)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=
# Local only: bypass Kong and call `supabase functions serve` directly (fix 503). Use port from "Serving functions on ..." (often 8081).
# VITE_SUPABASE_FUNCTIONS_DIRECT_URL=http://127.0.0.1:8081

# --- Edge function secrets — set in Supabase Dashboard → Edge Functions → Secrets ---
# Invite emails (Resend). Use a verified-domain address so you can send to any recipient.
RESEND_API_KEY=
RESEND_FROM=
# App URL for invite and engagement links (e.g. https://yourdomain.com)
PUBLIC_APP_URL=
APP_URL=

# Talent Sourcing: Web Search / LinkedIn
FIRECRAWL_API_KEY=
GOOGLE_CSE_API_KEY=
GOOGLE_CSE_CX=
SERPAPI_API_KEY=# AI (resume/job parsing, matching, etc.)
OPENAI_API_KEY=
OPENAI_MODEL=# Engagement emails (SMTP; optional — e.g. Resend SMTP or your provider)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_TLS=
SMTP_FROM=# RTR (Right to Represent) — fillable PDF from DOCX template (optional)
# Same solution on local and prod: set CLOUDCONVERT_API_KEY. CloudConvert is ISO 27001 certified, GDPR compliant; files are deleted after processing (see cloudconvert.com/security). Alternative: local LibreOffice (RTR_TEMPLATE_DOCX_PATH + soffice on PATH) or your own service (RTR_CONVERSION_SERVICE_URL).
#   RTR_TEMPLATE_DOCX_URL   – URL to fetch the DOCX (use in prod)
#   RTR_TEMPLATE_DOCX_PATH  – local path (e.g. docs/CompSciPrep_RTR_Template.docx) when running functions serve
RTR_TEMPLATE_DOCX_URL=
RTR_TEMPLATE_DOCX_PATH=
CLOUDCONVERT_API_KEY=
# Optional: your own DOCX→PDF service (POST /convert, body { docx_base64 }, response PDF or { pdf_base64 }).
# RTR_CONVERSION_SERVICE_URL=